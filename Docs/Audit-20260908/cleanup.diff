--- before/Assets/_Project/Scripts/ButtonTest.cs
+++ after/Assets/_Project/Scripts/ButtonTest.cs
@@ -1,5 +1,3 @@
-using System.Collections;
-using System.Collections.Generic;
 using UnityEngine;
 using UnityEngine.UI;
 
--- before/Assets/_Project/Scripts/CSVPrinter.cs
+++ after/Assets/_Project/Scripts/CSVPrinter.cs
@@ -3,7 +3,6 @@
 using UnityEngine;
 using System;
 using System.IO;
-using System.Text;
 
 public class CSVPrinter : MonoBehaviour
 {
@@ -211,11 +210,6 @@
         using StreamWriter streamwriter_CarData = writers.car;
         using StreamWriter streamwriter_HumanData = writers.human;
         using StreamWriter streamwriter_HumanData_InExperiment = writers.inExperiment;
-
-        /*for (int i = 0; i < x.Length; i++)
-        {
-            streamwriter.WriteLine(x[i].ToString());
-        }*/
 
         //被験者の情報データを書き出し
         //1行目部分
@@ -343,10 +337,6 @@
         this.CarDataList = new List<CarData>();
         this.HumanDataList = new List<HumanData>();
 
-        //csvデータファイルを作成
-
-        /*logData = new int[] { 1, 3, 5, 7, 9 }; // 仮のLogデータ
-        LogSave(logData, "logData"); // Logデータをcsv形式で書き出す*/
     }
 
     // Update is called once per frame
--- before/Assets/_Project/Scripts/CenterEyeCamera.cs
+++ after/Assets/_Project/Scripts/CenterEyeCamera.cs
@@ -123,54 +123,14 @@
             return;
         }
 
-        /*//AudioComponentを取得
-        audioSource = GetComponent<AudioSource>();
-
-        // 波形を作成 (2 秒間で 96000 サンプル)
-        float[] waveform = new float[96000];
-        for (int sample = 0; sample < 96000; sample++)
-        {
-            //10サンプルで、-1と+1を半々(約4800ヘルツ)
-            waveform[sample] = (sample % 10 < 5) ? +1.0f : -1.0f;
-        }*/
-
         //不快音をストップ
         audioSource.Stop();
 
         Hz = Sound;
-
-        /*int sampleRate = 48000;
-        float durationSeconds = 2f;
-
-        int sampleCount = Mathf.CeilToInt(sampleRate * durationSeconds);
-        float[] waveform = new float[sampleCount];
-
-        int samplesPerCycle = Mathf.FloorToInt(sampleRate / Hz);
-        int halfCycle = samplesPerCycle / 2;
-
-        for (int i = 0; i < sampleCount; i++)
-        {
-            // 矩形波：周期の前半+1、後半-1
-            waveform[i] = (i % samplesPerCycle < halfCycle) ? +1.0f : -1.0f;
-        }*/
 
         int sampleRate = 48000;
         float durationSeconds = 2f;
         float[] waveform = CreateToneWaveform(Hz, sampleRate, durationSeconds, 0.22f);
-
-        /*// 任意の周波数（例: 5000‾16000Hz など）
-        float frequency = Hz; // 例: Hz = 5000‾16000
-        float phase = 0f;
-        float phaseIncrement = frequency / sampleRate;  // 毎サンプルの位相増分 (0‾1)
-
-        // 高精度矩形波生成
-        for (int i = 0; i < sampleCount; i++)
-        {
-            waveform[i] = (phase < 0.5f) ? 1f : -1f; // 位相が0‾0.5なら+1、0.5‾1.0なら-1（1周期）
-            phase += phaseIncrement;
-            if (phase >= 1f) phase -= 1f; // ループ
-        }*/
-
 
         // 波形を AudioClip オブジェクトに格納 (1 秒間あたり 48000 サンプル)
         DieNoise = AudioClip.Create("HighSound", waveform.Length, 1, sampleRate, false);
@@ -197,37 +157,9 @@
 
         Hz = Sound;
 
-        /*int sampleRate = 48000;
-        float durationSeconds = 2f;
-
-        int sampleCount = Mathf.CeilToInt(sampleRate * durationSeconds);
-        float[] waveform = new float[sampleCount];
-
-        int samplesPerCycle = Mathf.FloorToInt(sampleRate / Hz);
-        int halfCycle = samplesPerCycle / 2;
-
-        for (int i = 0; i < sampleCount; i++)
-        {
-            // 矩形波：周期の前半+1、後半-1
-            waveform[i] = (i % samplesPerCycle < halfCycle) ? +1.0f : -1.0f;
-        }*/
-
         int sampleRate = 48000;
         float durationSeconds = 2f;
         float[] waveform = CreateToneWaveform(Hz, sampleRate, durationSeconds, 0.22f);
-        // 任意の周波数（例: 5000‾16000Hz など）
-        /*float frequency = Hz; // 例: Hz = 5000‾16000
-        float phase = 0f;
-        float phaseIncrement = frequency / sampleRate;  // 毎サンプルの位相増分 (0‾1)
-
-        // 高精度矩形波生成
-        for (int i = 0; i < sampleCount; i++)
-        {
-            waveform[i] = (phase < 0.5f) ? 1f : -1f; // 位相が0‾0.5なら+1、0.5‾1.0なら-1（1周期）
-            phase += phaseIncrement;
-            if (phase >= 1f) phase -= 1f; // ループ
-        }*/
-
 
         // 波形を AudioClip オブジェクトに格納 (1 秒間あたり 48000 サンプル)
         DieNoise = AudioClip.Create("HighSound", waveform.Length, 1, sampleRate, false);
